[build]
  base = "dashboard"
  command = "npm run build"
  # Relative to `base`, so this is dashboard/dist.
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Single-page app: every route serves the same page.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
